import React, { forwardRef } from "react";

const ToysTypeSection = forwardRef((props, ref) => {
  return (
    <div ref={ref}>
      <h1>Toy Type Section</h1>
    </div>
  );
});

export default ToysTypeSection;
